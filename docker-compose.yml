services:
  shellcheck:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    volumes:
      - .:/workspace:ro
    command: >-
      sh -c 'find linux/scripts linux/install linux/config/quickshell/scripts -type f -print0 | xargs -0 shellcheck -S error'
