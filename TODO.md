# TODO

## Installation flow

* Classify the remaining imperative installers: `post`, `configs-system`, `icons`, and `kmonad-device-manager` are not run by `install/sync` or `install/all`. Decide whether each is required and should be integrated into the main flow, optional with documented prerequisites, or obsolete and removable.
