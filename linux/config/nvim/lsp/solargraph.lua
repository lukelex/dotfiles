return {
  cmd = { "solargraph", "stdio" },
  filetypes = { "ruby" },
  init_options = { formatting = false },
  settings = {
    solargraph = {
      diagnostics = false,
      completion = true,
      formatting = false
    }
  },
}
