#!/bin/sh

info=$(bluetoothctl show)

powered=$(echo "$info" | grep 'Powered: yes' | awk '{print $2}')
summary=$(bluetoothctl paired-devices | awk '{print $3 " (paired)"}')

dunstify -i bluetooth \
         -r 29 \
         -u low \
         -a Bluetooth \
         "$powered" "$summary"
